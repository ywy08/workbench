
帮我从零搭建移动端日常工作台，可以添加到手机主屏幕，像原生 App 一样使用

要求：

一、单文件网页，无后端，所有数据本地浏览器存储（localStorage）；支持 PWA，手机添加到桌面独立打开

二、UI 采用移动端 App 风格，左侧抽屉导航，橄榄绿主色调（#556B2F）、圆角卡片布局（16px 圆角）

三、把配置类数据（语录、模板等）推送到 GitHub Gist，网页默认读取 Gist 数据，实现跨设备同步。个人记录类数据（每日计划、打卡、复盘等）仍存本地

四、一共 9 大功能模块，需求如下：

1. 每日计划：今日任务列表（添加、完成、删除），支持"拉取计划"按钮从模板生成今日计划，完成任务显示完成状态

2. 选题灵感：灵感卡片（标题、标签、备注），支持添加、编辑、删除，按分类筛选

3. 二创方向：二创创意列表（标题、关联热点、思路），支持添加、编辑、删除，标记已完成状态

4. 内容复盘：今日复盘笔记（文本编辑），快速复盘模板（Q&A 形式），保存后可查看历史记录

5. 创作打卡：今日创作内容（标题、正文摘要），打卡统计（连续天数、总天数），历史打卡记录

6. 每日前端：技术新闻列表（标题、来源、链接），支持标记已读，快速记录学习笔记

7. 每日锻炼：锻炼动作列表（动作、组数、次数），今日完成状态，锻炼连续打卡统计

8. 股票基金：持仓列表（名称、代码、成本价），今日行情录入（现价），盈亏自动计算

9. 每日阅读：阅读清单（书名、进度、备注），今日阅读打卡，连续阅读统计

五、抽屉导航设计：
- 抽屉头部：圆形头像 + 名字"工作台" + 当前日期
- 头部下方显示简化的励志语录（单行显示）
- 导航项列表，无分组，当前选中项左侧绿色边框高亮
- 抽屉底部："管理语录 →" 链接，点击可添加/编辑/删除语录
- 首页不显示语录卡片，语录仅在抽屉头部展示

六、Gist 同步方案：
- 哪些数据存 Gist：励志语录库、每日计划模板、灵感分类标签、复盘问题模板、锻炼动作库
- 哪些数据不存 Gist：每日计划记录、打卡记录、复盘笔记、股票持仓、阅读进度（这些存本地）
- Gist 数据格式示例：
  {
    "version": "1.0",
    "quotes": [{ "text": "不要等待机会，而要创造机会。", "author": "萧伯纳" }],
    "planTemplates": ["晨间冥想 10 分钟", "核心任务 2 项", "阅读 30 分钟"],
    "inspirationCategories": ["技术", "生活", "产品"],
    "reviewQuestions": ["今天最有成就感的事是？", "明天可以改进的是？"],
    "exerciseLibrary": [{ "name": "俯卧撑", "sets": 3, "reps": 15 }]
  }
- 同步策略：启动时读取 Gist 合并到本地（Gist 优先），可手动同步

七、默认数据：
- 默认语录 12 条（励志类）
- 默认计划模板：晨间冥想、核心任务、阅读、运动、总结复盘

八、技术实现要点：
- PWA 支持：manifest.json + service-worker.js，缓存核心资源，离线可用
- 移动端适配：viewport meta、apple-mobile-web-app-capable、自定义图标
- 文件结构：workbench.html（主文件）、manifest.json、service-worker.js

九、部署方式：
- 本地预览：python -m http.server 8080
- 手机访问：同局域网电脑 IP + 端口
- 公网部署：GitHub Pages / Vercel / Netlify
